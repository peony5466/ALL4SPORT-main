{% extends 'base.html.twig' %}

{% block title %}Hello RayonController!{% endblock %}

{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; text-align:center; }
</style>
<header>
{#<nav class="onglet">
  <a class="acceuil" href="{{ path('app_acceuil')}}">Acceuil</a>
  <a class="rayon" href="{{ path('app_acceuil') }}">Rayon</a>
  <a class="panier" href="{{ path('app_commande') }}">Panier</a>
  <a class="produits" href="{{ path('app_produits') }}">Produits</a>
  <ul>
  <button onclick="se connecter">se connecter</button>
</nav>
</ul>
</header>#}

<div class="example-wrapper">
{% for rayon in rayons %}
  <a href="{{ path('app_produits', {'id': rayon.id}) }}"><h3>{{rayon.libelle}}</h3></a>
    
{% endfor %}

 

        

</div>
{% endblock %}